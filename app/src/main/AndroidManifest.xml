<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.com.enforce.hippov1">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".MainActivity" />
        <activity android:name=".Categorias" />
        <activity android:name=".ProdutoCategoria" />
        <activity android:name=".Produto" />
        <activity android:name=".DescricaoProduto" />
        <activity android:name=".rest.Cadastro" />
        <activity android:name=".Login" />
        <activity android:name=".Carrinho" />
        <activity android:name=".Pagamento" />
        <activity android:name=".ReadQr" />
        <activity android:name=".Sobre"
            android:label="@string/title_activity_about"
            android:theme="@style/AppTheme.NoActionBar">
        </activity>

    </application>

</manifest>